import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        int n = scanner.nextInt();
        while(n-- > 0){
            int[] board = new int[scanner.nextInt()];
            Arrays.fill(board, 1);
            boolean aliceWins = playNimGame(board);

            if (!aliceWins) {
                System.out.println("Alice");
            } else {
                System.out.println("Bob");
            }
        }

    }

    public static boolean playNimGame(int[] board) {
        int n = board.length;
        int xorSum = 0;

        for (int num : board) {
            xorSum ^= num;
        }

        if (xorSum == 0) {
            return false;
        }

        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++) {
                int sum = board[i] + board[j];
                if (board[i] == board[j] && (xorSum ^ board[i]) < xorSum) {
                    int[] newBoard = createNewBoard(board, i, j, sum);
                    if (!playNimGame(newBoard)) {
                        return true;
                    }
                }
            }
        }

        return false;
    }

    public static int[] createNewBoard(int[] board, int index1, int index2, int sum) {
        int[] newBoard = new int[board.length - 1];
        int newIndex = 0;

        for (int i = 0; i < board.length; i++) {
            if (i != index1 && i != index2) {
                newBoard[newIndex++] = board[i];
            }
        }

        newBoard[newIndex] = sum;
        return newBoard;
    }
}
